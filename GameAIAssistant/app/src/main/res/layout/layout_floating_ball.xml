<?xml version="1.0" encoding="utf-8"?>
<!-- 悬浮球布局：评分模式52dp圆形 / 语音模式200dp圆角矩形 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="52dp"
    android:layout_height="52dp"
    android:minWidth="52dp"
    android:minHeight="52dp"
    android:padding="0dp">

    <!-- 背景 — 代码根据模式切换 drawable -->
    <View
        android:id="@+id/bg_circle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_floating_ball" />

    <!-- ═══════════ 评分模式（默认显示）═══════════ -->
    <LinearLayout
        android:id="@+id/layout_score_mode"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/tv_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:shadowColor="#000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="5" />
    </LinearLayout>

    <!-- 评级标签（评分模式可见） -->
    <TextView
        android:id="@+id/tv_grade"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="-5dp"
        android:text="S"
        android:textColor="#080C1A"
        android:textSize="10sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/bg_grade_circle" />

    <!-- ═══════════ 语音模式 — 紧凑圆形指示 ═══════════ -->
    <LinearLayout
        android:id="@+id/layout_voice_mode"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:clickable="false"
        android:focusable="false"
        android:visibility="gone">

        <!-- 脉冲点 -->
        <View
            android:id="@+id/voice_pulse_dot"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:layout_marginBottom="2dp"
            android:background="@drawable/bg_voice_pulse" />

        <!-- 状态文字（聆听中/思考中/播报中） -->
        <TextView
            android:id="@+id/tv_voice_mode_text"
            android:layout_width="42dp"
            android:layout_height="wrap_content"
            android:text="聆听中"
            android:textColor="@color/accent_primary"
            android:textSize="8sp"
            android:textStyle="bold"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 当前轮文字摘要（用户说话或AI播报时简短显示） -->
        <TextView
            android:id="@+id/tv_voice_cur_text"
            android:layout_width="44dp"
            android:layout_height="wrap_content"
            android:textColor="#AAE8ECF1"
            android:textSize="7sp"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="1dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- 麦克风按钮 — 始终可见 -->
    <TextView
        android:id="@+id/btn_mic"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="-2dp"
        android:layout_marginBottom="-2dp"
        android:gravity="center"
        android:text="🎤"
        android:textSize="10sp"
        android:background="@drawable/bg_grade_circle"
        android:clickable="true"
        android:focusable="true" />
</FrameLayout>
