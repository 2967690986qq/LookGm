<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#080C1A"
    android:orientation="vertical">

    <!-- 顶部栏：标题 + 模型切换 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:background="#0A0E1E">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="AI 对话"
            android:textColor="#E8ECF1"
            android:textSize="18sp"
            android:textStyle="bold" />

        <LinearLayout
            android:id="@+id/ll_model_switcher"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="10dp"
            android:paddingEnd="8dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/bg_chat_input">

            <TextView
                android:id="@+id/tv_selected_model"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="选择模型"
                android:textColor="#00E5FF"
                android:textSize="12sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:maxWidth="100dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=" ▾"
                android:textColor="#00E5FF"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- 语音对话状态条 -->
    <LinearLayout
        android:id="@+id/ll_voice_status"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="#0C1122"
        android:visibility="gone">

        <View
            android:id="@+id/v_voice_dot"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:background="@drawable/bg_streaming_dot"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/tv_voice_state"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="正在聆听..."
            android:textColor="#00E5FF"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/btn_voice_stop"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:gravity="center"
            android:text="停止"
            android:textColor="#FF4444"
            android:textSize="11sp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/bg_chat_input" />
    </LinearLayout>

    <!-- 消息列表 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_messages"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        android:scrollbars="none" />

    <!-- 底部输入栏 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="10dp"
        android:background="#0A0E1E">

        <!-- 语音按钮 -->
        <ImageView
            android:id="@+id/iv_mic"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_mic"
            android:padding="10dp"
            android:background="@drawable/bg_chat_input"
            android:clickable="true"
            android:focusable="true" />

        <!-- 文本输入 -->
        <EditText
            android:id="@+id/et_input"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_chat_input"
            android:hint="输入消息..."
            android:textColorHint="#505A6E"
            android:textColor="#E8ECF1"
            android:textSize="14sp"
            android:paddingStart="16dp"
            android:paddingEnd="12dp"
            android:maxLines="3"
            android:inputType="textMultiLine|textCapSentences" />

        <!-- 发送按钮 -->
        <ImageView
            android:id="@+id/iv_send"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_send"
            android:padding="10dp"
            android:background="@drawable/bg_chat_bubble_ai"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>
</LinearLayout>
